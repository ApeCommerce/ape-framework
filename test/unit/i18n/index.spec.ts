import 'test/unit/config';
// import { loadModule } from 'utils';

describe('Initializing and getting the I18n instance', () => {
  test('Returns the same instance', async () => {
    expect(false).toBeTruthy();
  });
});
